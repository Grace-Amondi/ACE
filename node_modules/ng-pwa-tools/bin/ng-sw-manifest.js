#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var minimist = require("minimist");
var fs = require("fs");
var args_1 = require("../lib/common/args");
var util_1 = require("../lib/common/util");
var args = minimist(process.argv.slice(2), {
    string: ['in', 'module', 'base-href', 'dist']
});
args_1.requiredArgs(args, ['dist', 'module']);
var dist = util_1.toAbsolute(args['dist']);
var module = util_1.toAbsolute(args['module']);
var baseHref = module['base-href'] || '/';
var inFile = args['in'];
var manifest = {};
if (inFile) {
    manifest = JSON.parse(fs.readFileSync(util_1.toAbsolute(inFile)).toString());
}
console.log(JSON.stringify(manifest, null, 2));
